<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<title>{{site.title}}</title>
    <link href="{{site.url}}/atom.xml" rel="self" />
    <link href="{{site.url}}" />
  	<updated>{{date.toISOString()}}</updated>

    {% for p in posts %}
        <entry>
			{% if p.subtitle %}
				<title><![CDATA[{{p.title}}. {{p.subtitle}}]]></title>
			{% else %}
            	<title><![CDATA[{{p.title}}]]></title>
			{% endif %}
            <link href="{{site.url}}/{{p.path}}" />
            <updated>{{p.updated.toISOString() if p.updated else p.date.toISOString()}}</updated>
            <summary><![CDATA[{{p.less}}]]></summary>
        </entry>
    {% endfor %}
</feed>
