{% macro tagList(tags) %}
    <nav>
        {% for t in tags %}
            <a href="{{site.root}}/tags/{{t.slug}}">{{t.name}}</a>{% if not loop.last %},{% endif %}
        {% endfor %}
    </nav>
{% endmacro %}
