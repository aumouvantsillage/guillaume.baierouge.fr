{% extends "page.njk" %}
{% from "post-list.njk" import postList %}

{% block main %}
    <header>
	    <h1>Posts for tag: {{tag}}</h1>
    </header>
    {{postList(pagination.files)}}
{% endblock %}
